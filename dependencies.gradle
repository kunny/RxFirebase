ext {
    // Libraries

    autoValueVersion = '1.5'
    assertjVersion = '1.2.0'
    firebaseAuthVersion = '15.1.0'
    firebaseCoreVersion = '15.0.0'
    firebaseDatabaseVersion = '15.0.0'
    junitVersion = '4.12'
    kotlinVersion = '1.2.41'
    mockitoVersion = "2.13.0"
    mockitoKotlinVersion = "1.5.0"
    rxJavaVersion = '2.1.8'

    // Build plugins

    androidPluginVersion = '3.1.3'
    versionMatcherPluginVersion = '1.0.2'

    // Build SDK versions

    buildToolsVersion = '27.0.3'
    minSdkVersion = 14
    compileSdkVersion = 27

    // CI

    ci = 'true'.equals(System.getenv('CI'))
}
