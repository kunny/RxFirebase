ext {
    // Libraries

    autoValueVersion = '1.5'
    assertjVersion = '1.2.0'
    firebaseAuthVersion = '12.0.0'
    firebaseCoreVersion = '12.0.0'
    firebaseDatabaseVersion = '12.0.0'
    junitVersion = '4.12'
    kotlinVersion = '1.2.41'
    mockitoVersion = "2.13.0"
    mockitoKotlinVersion = "1.5.0"
    rxJavaVersion = '2.1.8'

    // Build plugins

    androidPluginVersion = '3.0.1'

    // Build SDK versions

    buildToolsVersion = '27.0.1'
    minSdkVersion = 14
    compileSdkVersion = 27

    // CI

    ci = 'true'.equals(System.getenv('CI'))
}
